# =============================================================================
# SoftMusic — Docker Compose overlay de produção
# =============================================================================
# Uso:
#   docker compose -f infra/docker/docker-compose.yml \
#     -f infra/docker/docker-compose.prod.yml \
#     --env-file /opt/softmusic/.env.production \
#     --profile infra --profile app --profile observability up -d
# =============================================================================

services:
  mysql:
    restart: unless-stopped
    ports:
      - "3307:3307"
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G

  redis:
    restart: unless-stopped
    ports: !reset []
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M

  rabbitmq:
    restart: unless-stopped
    ports: !reset []
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1G

  api:
    restart: unless-stopped
    image: ${SOFTMUSIC_API_IMAGE:-softmusic/api:latest}
    pull_policy: never
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 512M

  python-ai:
    restart: unless-stopped
    image: ${SOFTMUSIC_PYTHON_AI_IMAGE:-softmusic/python-ai:latest}
    pull_policy: never
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 8G
        reservations:
          cpus: "1"
          memory: 2G

  worker:
    restart: unless-stopped
    image: ${SOFTMUSIC_PYTHON_AI_IMAGE:-softmusic/python-ai:latest}
    pull_policy: never
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 8G
        reservations:
          cpus: "1"
          memory: 2G

  web:
    restart: unless-stopped
    image: ${SOFTMUSIC_WEB_IMAGE:-softmusic/web:latest}
    pull_policy: never
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 256M

  lp:
    restart: unless-stopped
    image: ${SOFTMUSIC_LP_IMAGE:-softmusic/lp:latest}
    pull_policy: never
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M

  admin-web:
    restart: unless-stopped
    image: ${SOFTMUSIC_ADMIN_WEB_IMAGE:-softmusic/admin-web:latest}
    pull_policy: never
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M

  scheduler:
    restart: unless-stopped
    image: ${SOFTMUSIC_PYTHON_AI_IMAGE:-softmusic/python-ai:latest}
    pull_policy: never
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M

  nginx:
    profiles: [app]
    image: nginx:1.27-alpine
    container_name: softmusic-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ${SOFTMUSIC_NGINX_DIR:-../nginx}/nginx.conf:/etc/nginx/nginx.conf:ro
      - ${SOFTMUSIC_NGINX_DIR:-../nginx}/conf.d:/etc/nginx/conf.d:ro
      - softmusic_certbot_certs:/etc/letsencrypt:ro
    depends_on:
      api:
        condition: service_healthy
      web:
        condition: service_healthy
      lp:
        condition: service_started

  certbot:
    profiles: [app]
    image: certbot/certbot:latest
    container_name: softmusic-certbot
    restart: unless-stopped
    volumes:
      - softmusic_certbot_certs:/etc/letsencrypt
      - softmusic_certbot_www:/var/www/certbot
    entrypoint: /bin/sh -c "trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done"

volumes:
  softmusic_certbot_certs:
  softmusic_certbot_www:
